/**
 * Imports Native Components
 */
import { StyleSheet } from "react-native";

/**
 * Creates the styles
 */
export const styles = StyleSheet.create({
  timingButton: {
    flex: 1,
    alignItems: "center",
  },
});
